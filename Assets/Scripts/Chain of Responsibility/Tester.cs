using System;
using UnityEngine;

public class Tester : MonoBehaviour
{
	void Start() {
		//ILink unity/*, file*/;
		//unity = new UnityConsoleLogger(LinkLevel.All);
		////file = unity.SetNext(new FileLogger(LinkLevel.All, "test.txt"));

		//unity.Request("This is a test!", LinkLevel.Info);
	}
}
